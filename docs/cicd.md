# Managing hosts through CI/CD

## Syncing a host

This is done automatically on every push of a new commit.
To trigger this workflow manually, visit `Actions > Deploy > Run workflow`.

## Resetting a host

This only works for existing nixos hosts, not new ones.

To reset pawhost-next, visit `Actions > Reset-next > Run workflow`.